from setuptools import setup

setup(name='statplot',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['statplot'],
      author =['Godspower Onyenanu'],
      zip_safe=False)
